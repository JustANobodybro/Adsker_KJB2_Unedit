local args = {
    [1] = "LoadChar"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local args = {
    [1] = "ChangeValue",
    [2] = game:GetService("Players").LocalPlayer.PlayerGui.NoResetOnSpawn:WaitForChild("SelctionChar"),
    [3] = "Megumi Fshiguro"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

 task.wait(0.5)
 repeat wait() until game.Players.LocalPlayer.Character
 task.wait(0.5)
 
 for _, tool in ipairs(game.Players.LocalPlayer.Backpack:GetDescendants()) do
   if tool:IsA("Tool") then
tool:Destroy()
   end
 end
 
 game:GetService("StarterGui"):SetCoreGuiEnabled(Enum.CoreGuiType.Backpack, true)
 
 game.Players.LocalPlayer:SetAttribute("NoHit", false)
 game.Players.LocalPlayer:SetAttribute("MSlowed", false)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
 game.Players.LocalPlayer:SetAttribute("IsAwak", false)
 
 game.Players.LocalPlayer:GetAttributeChangedSignal("NoHit"):Connect(function()
local noh = game.Players.LocalPlayer:GetAttribute("NoHit")
  if noh then
local args = {
    [1] = "ChangeAtribbe",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name),
    [3] = "CantHit",
    [4] = true
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  else
local args = {
    [1] = "ChangeAtribbe",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name),
    [3] = "CantHit",
    [4] = false
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
 end)
 
 game.Players.LocalPlayer:GetAttributeChangedSignal("MSlowed"):Connect(function()
local msl = game.Players.LocalPlayer:GetAttribute("MSlowed")
  if msl then
repeat game:GetService("RunService").Heartbeat:Wait()
local args = {
    [1] = "ChangeAtribbe",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name),
    [3] = "SpeedChanger",
    [4] = 0.1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
until game.Players.LocalPlayer:GetAttribute("MSlowed") == false or game.Players.LocalPlayer.Character.Humanoid.Health <= 0
   else
local args = {
    [1] = "ChangeAtribbe",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name),
    [3] = "SpeedChanger",
    [4] = 0
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
 end)
 
 local function Cut(bool)
  for _, tool in ipairs(game.Players.LocalPlayer.Character:GetDescendants()) do
   if tool:IsA("Tool") then
tool.RequiresHandle = bool
   end
  end
  
  for _, tool in ipairs(game.Players.LocalPlayer.Backpack:GetDescendants()) do
   if tool:IsA("Tool") then
tool.RequiresHandle = bool
   end
  end
 end
 
 game.Players.LocalPlayer:GetAttributeChangedSignal("CanUsingTools"):Connect(function()
local CUT = game.Players.LocalPlayer:GetAttribute("CanUsingTools")
  if CUT then
Cut(false)
  else
Cut(true)
  end
 end)
 
local back = Instance.new("Part", game.Players.LocalPlayer.Character:FindFirstChild("Torso"))
back.CanCollide = false
back.Size = Vector3.new(2, 2, 0.5)
back.CFrame = game.Players.LocalPlayer.Character:FindFirstChild("Torso").CFrame * CFrame.Angles(1, math.rad(0), 0)

local attach = Instance.new("Weld", game.Players.LocalPlayer.Character:FindFirstChild("Torso"))
attach.Part0 = back
attach.Part1 = game.Players.LocalPlayer.Character:FindFirstChild("Torso")
attach.C0 = CFrame.new(0, 0, -0.75) * CFrame.Angles(0, 0, 40) 

local sworddec = Instance.new("SpecialMesh", back)
sworddec.TextureId = "rbxassetid://83509797"
sworddec.MeshId = "rbxassetid://83509558"

local sword = Instance.new("Part", workspace)
sword.CanCollide = false
sword.Transparency = 1
sword.Size = Vector3.new(0.1, 0.1, 0.1)

local sattach = Instance.new("Weld", sword)
sattach.Part0 = sword
sattach.Part1 = game.Players.LocalPlayer.Character:FindFirstChild("Right Arm")
sattach.C0 = CFrame.new(-1, -2.5, 0) * CFrame.Angles(math.rad(90), math.rad(0), math.rad(90))

local swordM = sworddec:Clone()
swordM.Parent = sword

local function breakJ(character)
 for _, joint in ipairs(character:GetDescendants()) do
  if joint:IsA("Weld") or joint:IsA("Motor6D") then
local args = {
    [1] = "DestroySomeThinggg",
    [2] = joint
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
 end
 
sound = Instance.new("Sound", character.Humanoid.RootPart)
sound.SoundId = "rbxassetid://531421188"
sound.Volume = 2.4
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()
end
 
local tool = Instance.new("Tool")
tool.Name = "Speed Dash"
tool.RequiresHandle = false

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 0.25 then
                lastPressTime = currentTime 
local partTweenn2 = game:GetService("TweenService"):Create(game.Players.LocalPlayer.Character.HumanoidRootPart, TweenInfo.new(0.05, Enum.EasingStyle.Exponential), {
        CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -25)
    })
    partTweenn2:Play()
 task.wait(0.05)
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("CleaveDance"):WaitForChild("TP"),
    [3] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Trembling"
tool.RequiresHandle = false
tool.ToolTip = "Cain becomes invulnerable to attacks for 2 seconds"

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 8 then
                lastPressTime = currentTime 
local Def = true
spookyn = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
spookyn.SoundId = "rbxassetid://1836983664"
spookyn.Volume = 4
spookyn.PlaybackSpeed = 0.95
spookyn.Looped = false
spookyn:Play()
 
spawn(function()
 repeat task.wait()
local TweenService = game:GetService("TweenService")
local character = game.Players.LocalPlayer.Character
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    if humanoidRootPart then
        local newCFrame = humanoidRootPart.CFrame * CFrame.new(math.random(-1, 1) / 4, 0, math.random(-1, 1) / 4)
        
        local tweenInfo = TweenInfo.new(
            0.01,
            Enum.EasingStyle.Linear,
            Enum.EasingDirection.Out,
            0,
            true,
            0
        )
        local tween = TweenService:Create(humanoidRootPart, tweenInfo, {CFrame = newCFrame})
        
        tween:Play()
        tween.Completed:Wait()
     end
 until Def == false
end)

game.Players.LocalPlayer:SetAttribute("NoHit", true)
 task.wait(2)
local partTweenn2 = game:GetService("TweenService"):Create(spookyn, TweenInfo.new(1, Enum.EasingStyle.Linear), {
        Volume = 0
    })
    partTweenn2:Play()
Def = false
game.Players.LocalPlayer:SetAttribute("NoHit", false)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

 local tool = Instance.new("Tool")
tool.Name = "Flash Step"
tool.RequiresHandle = false

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 4 then
                lastPressTime = currentTime 
 sword.Transparency = 1
 back.Transparency = 0
 game.Players.LocalPlayer:SetAttribute("NoHit", true)
 game.Players.LocalPlayer:SetAttribute("MSlowed", true)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://119242563245629"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
  task.wait(0.25)
  game.Players.LocalPlayer:SetAttribute("MSlowed", false)
 game.Players.LocalPlayer:SetAttribute("NoHit", false)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://6128977275"
sound.Volume = 2.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local foundHumanoids = {}
local radius = 10
local player = game.Players.LocalPlayer
local origin = player.Character.HumanoidRootPart.Position
local direction = player.Character.HumanoidRootPart.CFrame.LookVector

for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= player.Character then
        local humanoidPosition = v.RootPart.Position
        local toHumanoid = humanoidPosition - origin
        local distance = toHumanoid.magnitude
        local angle = math.acos(toHumanoid.Unit:Dot(direction))

        if distance <= 25 and angle < math.rad(30) then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
sound = Instance.new("Sound", humanoid.RootPart)
sound.SoundId = "rbxassetid://9075325599"
sound.Volume = 1.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

 for i = 1, 3 do
local args = {
    [1] = "SweepKickKick",
    [2] = humanoid.Parent
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
 end
   if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://120966577361845"
sound.Volume = 2.5
sound.PlaybackSpeed = 1
sound.TimePosition = 1.95
sound.Looped = false
sound:Play()

local args = {
    [1] = "RuthlessRoarHittt",
    [2] = humanoid.Parent,
    [3] = 53475753578654310,
    [4] = 100,
    [5] = 20,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   end
  task.wait(0.15)
   if humanoid.Health <= 0 then
spawn(function()
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local humanoid = game.Players.LocalPlayer.Character.Humanoid
local originalOffset = humanoid.CameraOffset

local startShakeIntensity = 15
local shakeSpeed = 0.01
local shakeDuration = 35

for i = 1, shakeDuration do
 task.wait()
 local shakeIntensity = startShakeIntensity * (1 - (i-1)/shakeDuration)
    local shakeOffset = Vector3.new(
     math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10
 )
 local originalOffset = humanoid.CameraOffset
 local currentTime = 0
 local duration = shakeSpeed
 while currentTime < duration do
     local delta = currentTime/duration
     humanoid.CameraOffset = originalOffset + shakeOffset * (1 - delta)
     currentTime = currentTime + task.wait(0.025)
 end
 humanoid.CameraOffset = Vector3.new(0, 0, 0)
end
end)

local args = {
    [1] = "StoicBombHitt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://3183222154"
sound.Volume = 3
sound.TimePosition = 0.1
sound.PlaybackSpeed = 0.9
sound.Looped = false
sound:Play()

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://7128851174"
sound.Volume = 2.5
sound.PlaybackSpeed = 0.95
sound.Looped = false
sound:Play()
 end
end
local partTweenn2 = game:GetService("TweenService"):Create(game.Players.LocalPlayer.Character.HumanoidRootPart, TweenInfo.new(0.05, Enum.EasingStyle.Exponential), {
        CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -25)
    })
    partTweenn2:Play()
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Uppercut"
tool.RequiresHandle = false

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 3 then
                lastPressTime = currentTime
sword.Transparency = 1
back.Transparency = 0
game.Players.LocalPlayer:SetAttribute("NoHit", true)
game.Players.LocalPlayer:SetAttribute("MSlowed", true)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://17751520382"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
  task.wait(0.6)
  game.Players.LocalPlayer:SetAttribute("MSlowed", false)
  game.Players.LocalPlayer:SetAttribute("NoHit", false)
  game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
k:AdjustSpeed(2)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity += Vector3.new(0, 75, 0)

local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= 13 then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
 for i = 1, 3 do
local args = {
    [1] = "UpperCut",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 0.7166666388511658
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

   if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://120966577361845"
sound.Volume = 2.5
sound.PlaybackSpeed = 1
sound.TimePosition = 1.95
sound.Looped = false
sound:Play()

local args = {
    [1] = "RuthlessRoarHittt",
    [2] = humanoid.Parent,
    [3] = 53475753578654310,
    [4] = 155, 
    [5] = 25,
    [6] = 2
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   end
 end
   task.wait(0.15)
    if humanoid.Health <= 0 then
spawn(function()
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local humanoid = game.Players.LocalPlayer.Character.Humanoid
local originalOffset = humanoid.CameraOffset

local startShakeIntensity = 15
local shakeSpeed = 0.01
local shakeDuration = 35

for i = 1, shakeDuration do
 task.wait()
 local shakeIntensity = startShakeIntensity * (1 - (i-1)/shakeDuration)
    local shakeOffset = Vector3.new(
     math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10
 )
 local originalOffset = humanoid.CameraOffset
 local currentTime = 0
 local duration = shakeSpeed
 while currentTime < duration do
     local delta = currentTime/duration
     humanoid.CameraOffset = originalOffset + shakeOffset * (1 - delta)
     currentTime = currentTime + task.wait(0.025)
 end
 humanoid.CameraOffset = Vector3.new(0, 0, 0)
end
end)

local args = {
    [1] = "StoicBombHitt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://3183222154"
sound.Volume = 3
sound.TimePosition = 0.1
sound.PlaybackSpeed = 0.9
sound.Looped = false
sound:Play()

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://7128851174"
sound.Volume = 2.5
sound.PlaybackSpeed = 0.95
sound.Looped = false
sound:Play()
 end
end 
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Consecutive punches"
tool.RequiresHandle = false

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 3 then
                lastPressTime = currentTime 
 pcall(function()
sword.Transparency = 1
back.Transparency = 0
 end)
 game.Players.LocalPlayer:SetAttribute("NoHit", true)
 game.Players.LocalPlayer:SetAttribute("MSlowed", true)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://81075107307954"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1.3)
  task.wait(0.1)
for i = 1, 12 do
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 10
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://6128977275"
sound.Volume = 2.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local foundHumanoids = {}
local radius = 4
local player = game.Players.LocalPlayer
local origin = player.Character.HumanoidRootPart.Position
local direction = player.Character.HumanoidRootPart.CFrame.LookVector

for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= player.Character and v.Health > 0 then
        local humanoidPosition = v.RootPart.Position
        local toHumanoid = humanoidPosition - origin
        local distance = toHumanoid.magnitude
        local angle = math.acos(toHumanoid.Unit:Dot(direction))

        if distance <= 8 and angle < math.rad(30) then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
  if i % 2 == 0 then
    if humanoid.Health > 2 then
for i = 1, 2 do
  local args = {
      [1] = "Fury BarrageDamge",
      [2] = workspace:WaitForChild(humanoid.Parent.Name),
      [3] = "Hit2"
  }

  game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end

if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
   if i % 3 == 0 then
 local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end

local args = {
    [1] = "RuthlessRoarHittt",
    [2] = humanoid.Parent,
    [3] = 53475753578654310,
    [4] = 0,
    [5] = 10,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end

sound = Instance.new("Sound", humanoid.RootPart)
sound.SoundId = "rbxassetid://9075325599"
sound.Volume = 0.75
sound.PlaybackSpeed = 1.1
sound.Looped = false
sound:Play()
   else
spawn(function()
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local humanoid = game.Players.LocalPlayer.Character.Humanoid
local originalOffset = humanoid.CameraOffset

local startShakeIntensity = 15
local shakeSpeed = 0.01
local shakeDuration = 35

for i = 1, shakeDuration do
 task.wait()
 local shakeIntensity = startShakeIntensity * (1 - (i-1)/shakeDuration)
    local shakeOffset = Vector3.new(
     math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10
 )
 local originalOffset = humanoid.CameraOffset
 local currentTime = 0
 local duration = shakeSpeed
 while currentTime < duration do
     local delta = currentTime/duration
     humanoid.CameraOffset = originalOffset + shakeOffset * (1 - delta)
     currentTime = currentTime + task.wait(0.025)
 end
 humanoid.CameraOffset = Vector3.new(0, 0, 0)
end
end)

local args = {
    [1] = "StoicBombHitt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://3183222154"
sound.Volume = 3
sound.TimePosition = 0.1
sound.PlaybackSpeed = 0.9
sound.Looped = false
sound:Play()

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://7128851174"
sound.Volume = 2.5
sound.PlaybackSpeed = 0.95
sound.Looped = false
sound:Play()
 end
  end
end
  task.wait()
end

 if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= 12 then
            table.insert(foundHumanoids, v)
        end
    end
end

   for _, humanoid in pairs(foundHumanoids) do
local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 200,
    [5] = 45,
    [6] = 1.5
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://120966577361845"
sound.Volume = 2.5
sound.PlaybackSpeed = 1
sound.TimePosition = 1.95
sound.Looped = false
sound:Play()

local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   end 
 end

game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local function tools()
local tool = Instance.new("Tool")
tool.Name = "Slash Barrage"
tool.RequiresHandle = false

local function slashan(character)
spawn(function()
 for i = 1, 2 do
  local slash = Instance.new("Part", workspace)
  slash.Material = "Neon"
  slash.Anchored = true
  slash.CanCollide = false
  slash.Transparency = 0.5
  slash.Size = Vector3.new(0, 0.1, 0.1)
  slash.Color = Color3.new(1, 1, 1)
  slash.CFrame = character.Humanoid.RootPart.CFrame
  slash.CFrame = slash.CFrame * CFrame.Angles(math.rad(math.random(-360, 360)), 0, math.rad(math.random(-360, 360)))
  game.Debris:AddItem(slash, 0.7)
  
   if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
slash.Color = Color3.new(0.7, 0, 0)
   end
  
  partTweenn2 = game:GetService("TweenService"):Create(slash, TweenInfo.new(0.5, Enum.EasingStyle.Exponential), {
        Size = Vector3.new(20, 0.1, 0.1)
    })
    partTweenn2:Play()
 end
end)
end

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 5 then
                lastPressTime = currentTime
sword.Transparency = 0
back.Transparency = 1
game.Players.LocalPlayer:SetAttribute("NoHit", true)
game.Players.LocalPlayer:SetAttribute("MSlowed", true)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
local args = {
    [1] = "ChangeTojiWapeon",
    [2] = "MeguminSword"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://18313759973"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1.5)
 for i = 1, 10 do
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://4958430453"
sound.Volume = 1
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local foundHumanoids = {}
local radius = 12
local player = game.Players.LocalPlayer
local origin = player.Character.HumanoidRootPart.Position
local direction = player.Character.HumanoidRootPart.CFrame.LookVector

for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= player.Character then
        local humanoidPosition = v.RootPart.Position
        local toHumanoid = humanoidPosition - origin
        local distance = toHumanoid.magnitude
        local angle = math.acos(toHumanoid.Unit:Dot(direction))

        if distance <= 35 and angle < math.rad(30) then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
  pcall(function()
slashan(humanoid.Parent)
  for i = 1, 2 do
 local args = {
    [1] = "DarkSurgeHitttttt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 384923741622
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
    if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
 local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 0,
    [5] = 7,
    [6] = 0
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
    end
 end)
  end
   task.wait(0.03)
 end
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
   local foundHumanoids = {}
local radius = 15
local player = game.Players.LocalPlayer
local origin = player.Character.HumanoidRootPart.Position
local direction = player.Character.HumanoidRootPart.CFrame.LookVector

for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= player.Character then
        local humanoidPosition = v.RootPart.Position
        local toHumanoid = humanoidPosition - origin
        local distance = toHumanoid.magnitude
        local angle = math.acos(toHumanoid.Unit:Dot(direction))

        if distance <= 35 and angle < math.rad(30) then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
 pcall(function()
 local args = {
    [1] = "SweepKickKick",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 100,
    [5] = 15,
    [6] = 0.5
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
 end)
end
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Hail of cuts"
tool.RequiresHandle = false

local function damag(sond)
  if game.Players.LocalPlayer:GetAttribute("IsAwak") == false then
local args = {
  [1] = "DarkSurgeSlashhh"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   else
local args = {
  [1] = "TOPIMPORTANT_VFXEMITEER",
  [2] = game.ReplicatedStorage.VFX.Sukuna.CravingCombaination.Hit,
  [3] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
 local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= 14 then
            table.insert(foundHumanoids, v)
        end
    end
end

  for _, humanoid in pairs(foundHumanoids) do
    pcall(function()
    for i = 1, 5 do
local args = {
    [1] = "DarkSurgeHitttttt",
    [2] = humanoid.Parent,
    [3] = 384923741622
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end 
  
if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 75,
    [5] = 15,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end
  end)
end

  if sond == 1 then
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://18465799826"
sound.Volume = 2.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()
  else
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://18465818815"
sound.Volume = 1.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()
  end
 end

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 8 then
                lastPressTime = currentTime
sword.Transparency = 0
back.Transparency = 1
game.Players.LocalPlayer:SetAttribute("NoHit", true)
game.Players.LocalPlayer:SetAttribute("MSlowed", true)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
local args = {
    [1] = "ChangeTojiWapeon",
    [2] = "MeguminSword"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://18468396479"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
 task.wait(0.8)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(1)
 task.wait(0.4)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(1)
 task.wait(0.3)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(1)
 task.wait(0.3)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(1)
 task.wait(0.3)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(1)
 task.wait(0.6)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(1)
 task.wait(0.7)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 175
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -10)
damag(2)
damag(2)
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Carnage Circle"
tool.RequiresHandle = false

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 7 then
                lastPressTime = currentTime
  pcall(function()
sword.Transparency = 0
back.Transparency = 1
  end)
  game.Players.LocalPlayer:SetAttribute("NoHit", true)
  game.Players.LocalPlayer:SetAttribute("MSlowed", true)
  game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
local args = {
    [1] = "ChangeTojiWapeon",
    [2] = "MeguminSword"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://18313527111"
sound.Volume = 2
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://18313759973"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(0.9)

local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= 80 then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
  pcall(function()
local args = {
    [1] = "BoulderCrushStunCharrr",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   end)
end 

local circle = Instance.new("MeshPart", workspace)
circle.Anchored = true
circle.MeshId = "rbxassetid://3270017"
circle.Color = Color3.new(1, 1, 1)
circle.Material = "Neon"
circle.CanCollide = false
circle.Size = Vector3.zero
circle.Position = game.Players.LocalPlayer.Character.HumanoidRootPart.Position
circle.CFrame = circle.CFrame * CFrame.Angles(math.rad(90), 0, 0)
game.Debris:AddItem(circle, 4)

 if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
circle.Color = Color3.new(0.7, 0, 0)
 end

local partTweenn2 = game:GetService("TweenService"):Create(circle, TweenInfo.new(0.3, Enum.EasingStyle.Circular), {
        Size = Vector3.new(100, 100, 10)
    })
    partTweenn2:Play()
 task.wait(1.1)
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://1837829564"
sound.Volume = 2
sound.PlaybackSpeed = 0.9
sound.Looped = false
sound:Play()

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://18313528958"
sound.Volume = 2
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local partTweenn2 = game:GetService("TweenService"):Create(circle, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {
        Size = Vector3.zero
    })
    partTweenn2:Play()

 spawn(function()
local humanoid = game.Players.LocalPlayer.Character.Humanoid
local originalOffset = humanoid.CameraOffset

local startShakeIntensity = 20
local shakeSpeed = 0.01
local shakeDuration = 30

for i = 1, shakeDuration do
 task.wait()
 local shakeIntensity = startShakeIntensity * (1 - (i-1)/shakeDuration)
    local shakeOffset = Vector3.new(
     math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10
 )
 local originalOffset = humanoid.CameraOffset
 local currentTime = 0
 local duration = shakeSpeed
 while currentTime < duration do
     local delta = currentTime/duration
     humanoid.CameraOffset = originalOffset + shakeOffset * (1 - delta)
     currentTime = currentTime + task.wait(0.025)
 end
 humanoid.CameraOffset = Vector3.new(0, 0, 0)
end
end)

local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= 80 then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
  pcall(function()
local args = {
    [1] = "BoulderCrushUNNNStunCharrr",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

 for i = 1, 16 do
local args = {
    [1] = "DarkSurgeHitttttt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 384923741622
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
 end

 for i = 1, 3 do
local args = {
    [1] = "SweepKickKick",
    [2] = workspace:WaitForChild(humanoid.Parent.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
    if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("CravingCombaination"):WaitForChild("Hit"),
    [3] = humanoid.RootPart.CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 200,
    [5] = 35,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args)) 
    end
 end)
end 
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Second wind"
tool.ToolTip = "I havent come up with an animation yet"
tool.RequiresHandle = false

local function tools2()
local tool = Instance.new("Tool")
tool.Name = "Ravaging Slash"
tool.RequiresHandle = false

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 5 then
                lastPressTime = currentTime 
   pcall(function()
sword.Transparency = 1
back.Transparency = 0
game.Players.LocalPlayer:SetAttribute("NoHit", true)
game.Players.LocalPlayer:SetAttribute("MSlowed", true)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
   end)
   
  local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://17751574832"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
  task.wait(1)
local circle = Instance.new("MeshPart", workspace)
circle.Anchored = true
circle.MeshId = "rbxassetid://3270017"
circle.Color = Color3.new(1, 1, 1)
circle.Material = "Neon"
circle.CanCollide = false
circle.Size = Vector3.zero
circle.Position = game.Players.LocalPlayer.Character.HumanoidRootPart.Position
circle.CFrame = circle.CFrame * CFrame.Angles(math.rad(90), 0, 0)
game.Debris:AddItem(circle, 4)

sound = Instance.new("Sound", game.Workspace)
sound.SoundId = "rbxassetid://7545764969" 
sound.Volume = 1
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://1837829564"
sound.Volume = 2
sound.PlaybackSpeed = 0.9
sound.Looped = false
sound:Play()

sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://18313528958"
sound.Volume = 2
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

spawn(function()
local humanoid = game.Players.LocalPlayer.Character.Humanoid
local originalOffset = humanoid.CameraOffset

local startShakeIntensity = 30
local shakeSpeed = 0.01
local shakeDuration = 30

for i = 1, shakeDuration do
 task.wait()
 local shakeIntensity = startShakeIntensity * (1 - (i-1)/shakeDuration)
    local shakeOffset = Vector3.new(
     math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10,
        math.random(-shakeIntensity, shakeIntensity) / 10
 )
 local originalOffset = humanoid.CameraOffset
 local currentTime = 0
 local duration = shakeSpeed
 while currentTime < duration do
     local delta = currentTime/duration
     humanoid.CameraOffset = originalOffset + shakeOffset * (1 - delta)
     currentTime = currentTime + task.wait(0.025)
 end
 humanoid.CameraOffset = Vector3.new(0, 0, 0)
end
end)

local args = {
    [1] = "MainModuleFUNCTIONN",
    [2] = "DestrouctionSystem",
    [3] = Vector3.new(200, 200, 200),
    [4] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= 100 then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
 local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 300,
    [5] = 80,
    [6] = 0.5
 }

 game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end 

partTweenn2 = game:GetService("TweenService"):Create(circle, TweenInfo.new(1, Enum.EasingStyle.Exponential), {
        Transparency = 1,
        Size = Vector3.new(500, 500, 35)
    })
    partTweenn2:Play()
 pcall(function()
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
 end)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Bloodbath" 
tool.ToolTip = "For this attack, Cain sacrifices his health"
tool.RequiresHandle = false

local function dmg(damage, knockback, range)
 local foundHumanoids = {}
Usuario = game.Players.LocalPlayer
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
        local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance <= range then
            table.insert(foundHumanoids, v)
        end
    end
end

  for _, humanoid in pairs(foundHumanoids) do
local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = knockback, -- kd
    [5] = damage, -- dmg
    [6] = 0.5
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end 
end

local function effect()
  local args = {
    [1] = "RuthlessLasttSlashh",
    [2] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -4)
  }

  game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end

local function hself(val)
  local args = {
    [1] = "HitSelfff",
    [2] = val
  }

  game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end

local function cutterrain(size)
 local args = {
    [1] = "MainModuleFUNCTIONN",
    [2] = "DestrouctionSystem",
    [3] = size,
    [4] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
 }

 game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
end

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 12 then
                lastPressTime = currentTime 
  pcall(function()
sword.Transparency = 1
back.Transparency = 0
game.Players.LocalPlayer:SetAttribute("NoHit", true)
game.Players.LocalPlayer:SetAttribute("MSlowed", true)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
  end)
  
  local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://79396764673442"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)

local args = {
    [1] = "Play",
    [2] = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.RootAttachment.Ruthless1,
    [3] = "Ruthless1"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local args = {
    [1] = "Play",
    [2] = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.RootAttachment.Ruthless2,
    [3] = "Ruthless2"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
 task.wait(0.5)
spawn(function()
   for i = 1, 5 do
local args = {
    [1] = "Chain of a thousand MilesGOOOO"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
dmg(10, 200, 30)
hself(1)
     task.wait(0.05)
   end
end)
 task.wait(1.5)
pcall(function()
 sword.Transparency = 0
 back.Transparency = 1
end)

local args = {
    [1] = "ChangeTojiWapeon",
    [2] = "MeguminSword"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

 for i = 0, 4 do
effect()
hself(1.5)
dmg(30 + i * 10, 60 + i * 10, 35)
cutterrain(Vector3.new(50, 50, 50))
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -(20 + i * 3))
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * (100 + i * 20)
   task.wait(0.4)
 end
 task.wait(0.8)
dmg(200, 300, 60)
hself(4)
cutterrain(Vector3.new(100, 100, 100))
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.new(0, 0, -40)
game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.LookVector * 200

local args = {
    [1] = "RuthlessLasttSlashh",
    [2] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -4)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

 pcall(function()
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
 end)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack
end

tool.Activated:Connect(function()
tool:Destroy()
  tools2()
  local args = {
    [1] = "ClairvoyanceAddAura",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local Highlight = Instance.new("Highlight")
Highlight.Parent = game.Players.LocalPlayer.Character
Highlight.Enabled = true
Highlight.Adornee = game.Players.LocalPlayer.Character
Highlight.FillTransparency = 0.9
Highlight.FillColor = Color3.fromRGB(128, 0, 0)
Highlight.OutlineColor = Color3.fromRGB(128, 0, 0)
Highlight.OutlineTransparency = 0.3
Highlight.DepthMode = "Occluded"
 game.Players.LocalPlayer:SetAttribute("IsAwak", true)
end)

tool.Parent = game.Players.LocalPlayer.Backpack
end

local tool = Instance.new("Tool")
tool.Name = "Cutting Punch"
tool.RequiresHandle = false 

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 4 then
                lastPressTime = currentTime 
    pcall(function()
  sword.Transparency = 1
  back.Transparency = 0
 game.Players.LocalPlayer:SetAttribute("NoHit", true)
 game.Players.LocalPlayer:SetAttribute("MSlowed", true)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
    end)
    
    local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
    
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://109303284105672"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(0.5)
  task.wait(0.2)
sound = Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
sound.SoundId = "rbxassetid://5713085119"
sound.Volume = 2
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local args = {
  [1] = "TOPIMPORTANT_VFXEMITEER",
  [2] = game.ReplicatedStorage.VFX.Sukuna.CravingCombaination.Hit,
  [3] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -6)
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local foundHumanoids = {}
local radius = 10
local player = game.Players.LocalPlayer
local origin = player.Character.HumanoidRootPart.Position
local direction = player.Character.HumanoidRootPart.CFrame.LookVector

for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= player.Character then
        local humanoidPosition = v.RootPart.Position
        local toHumanoid = humanoidPosition - origin
        local distance = toHumanoid.magnitude
        local angle = math.acos(toHumanoid.Unit:Dot(direction))

        if distance <= 12 and angle < math.rad(30) then
            table.insert(foundHumanoids, v)
        end
    end
end

for _, humanoid in pairs(foundHumanoids) do
  local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 75,
    [5] = 25,
    [6] = 1
  }

  game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
     spawn(function()
 for i = 1, 10 do
 local args = {
  [1] = "TOPIMPORTANT_VFXEMITEER",
  [2] = game.ReplicatedStorage.VFX.Sukuna.CravingCombaination.Hit,
  [3] = humanoid.RootPart.CFrame * CFrame.Angles(math.rad(math.random(-360, 360)), math.rad(math.random(-360, 360)), math.rad(math.random(-360, 360)))
 }

 game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

sound = Instance.new("Sound", humanoid.RootPart)
sound.SoundId = "rbxassetid://6881026094"
sound.Volume = 1.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()

local dma = 2
 if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
dma = 5
   else
dma = 2
 end

 local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(humanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 20,
    [5] = dma,
    [6] = 0.2
 }

 game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   task.wait(0.1)
 end
     end)
end
  task.wait(0.7)
      pcall(function()
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
      end)
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Flash Bang"
tool.RequiresHandle = false

local function playsound(id, parent)
sound = Instance.new("Sound", parent)
sound.SoundId = "rbxassetid://" .. tostring(id)
sound.Volume = 1.5
sound.PlaybackSpeed = 1
sound.Looped = false
sound:Play()
end

local lastPressTime = 0
tool.Activated:Connect(function()
  local currentTime = tick()
            if currentTime - lastPressTime >= 3 then
                lastPressTime = currentTime 
pcall(function()
 back.Transparency = 0
 sword.Transparency = 1
 game.Players.LocalPlayer:SetAttribute("NoHit", true)
 game.Players.LocalPlayer:SetAttribute("MSlowed", true)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
end)

local args = {
    [1] = "ChangeTojiWapeon",
    [2] = ""
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://76715054152443"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
  task.wait(0.3)
 playsound(5835032207, game.Players.LocalPlayer.Character.HumanoidRootPart)
local closestDistance = math.huge
local closestHumanoid = nil
local Usuario = game.Players.LocalPlayer

  for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character and v.Health > 0 then
      local distance = (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
        if distance < closestDistance and distance <= 10 then
             closestDistance = distance
             closestHumanoid = v
        end
     end
  end

if closestHumanoid and closestHumanoid.Health > 0 then
  if closestHumanoid.Health <= 0 then return end
 playsound(3201554385, game.Players.LocalPlayer.Character.HumanoidRootPart)
   spawn(function()
repeat wait()
  if closestHumanoid.Health <= 0 then
game.Players.LocalPlayer.Character.Humanoid.AutoRotate = true
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
  end
until closestHumanoid.Health <= 0
   end)
   
local K1 = 275
local D1 = 20
local D2 = 5
local D3 = 2.5
local K2 = 230
local D4 = 25
   
 if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
  K1 = 350
  D1 = 35
  D2 = 8
  D3 = 3
  K2 = 345
  D4 = 35
 end
 
local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(closestHumanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = K1,
    [5] = D1,
    [6] = 1.5
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Leg"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   end
 task.wait(0.5)
   if closestHumanoid.Health > 0 then
 repeat wait() until closestHumanoid.RootPart.Velocity.Magnitude <= 1 or closestHumanoid.Health <= 0
 game.Players.LocalPlayer.Character.Humanoid.AutoRotate = false
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://104240687221181"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
  task.wait(0.3)
local partTweenn2 = game:GetService("TweenService"):Create(game.Players.LocalPlayer.Character.HumanoidRootPart, TweenInfo.new(0.04, Enum.EasingStyle.Exponential), {
        CFrame = closestHumanoid.RootPart.CFrame * CFrame.new(0, 0, 3)
    })
    partTweenn2:Play()
  task.wait(0.08)   
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("CleaveDance"):WaitForChild("TP"),
    [3] = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  end
 for i = 1, 2 do
   if closestHumanoid.Health <= 0 then return end
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://17842945529"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
  task.wait(0.25)
 local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(closestHumanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 0,
    [5] = D2,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
playsound(159504677, closestHumanoid.RootPart)

    if game.Players.LocalPlayer:GetAttribute("IsAwak") == true then
local args = {
    [1] = "TOPIMPORTANT_VFXEMITEER",
    [2] = game:GetService("ReplicatedStorage"):WaitForChild("VFX"):WaitForChild("Sukuna"):WaitForChild("RagingFinisher"):WaitForChild("blackflash"),
    [3] = game.Players.LocalPlayer.Character["Right Arm"].CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
    end
  task.wait(0.15)
end
  local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://75696593697191"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
   task.wait(0.25)
   
 task.delay(0.35, function()
pcall(function()
  game.Players.LocalPlayer.Character.Humanoid.AutoRotate = true
  game.Players.LocalPlayer:SetAttribute("NoHit", false)
  game.Players.LocalPlayer:SetAttribute("MSlowed", false)
  game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
end)
 end)

   spawn(function()
for i = 1, 10 do
 if closestHumanoid.Health <= 0 then return end
   pcall(function()
 slashan(closestHumanoid.Parent)
   end)
local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(closestHumanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = 55,
    [5] = D3,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
playsound(150315649, closestHumanoid.RootPart)
  task.wait(0.1)
end
 task.wait(0.5)
   if closestHumanoid.Health <= 0 then return end
local args = {
  [1] = "TOPIMPORTANT_VFXEMITEER",
  [2] = game.ReplicatedStorage.VFX.Sukuna.CravingCombaination.Hit,
  [3] = closestHumanoid.RootPart.CFrame
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))

local args = {
    [1] = "RuthlessRoarHittt",
    [2] = workspace:WaitForChild(closestHumanoid.Parent.Name),
    [3] = 53475753578654310,
    [4] = K2,
    [5] = D4,
    [6] = 1
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
playsound(264486467, closestHumanoid.RootPart)

local args = {
    [1] = "DownCut",
    [2] = workspace:WaitForChild(closestHumanoid.Parent.Name),
    [3] = 0.7166666388511658
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
    end)
  else
 task.wait(0.5)
    pcall(function()
game.Players.LocalPlayer:SetAttribute("NoHit", false)
game.Players.LocalPlayer:SetAttribute("MSlowed", false)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
    end)
end
            end
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Sword of Doomsday"
tool.RequiresHandle = false

tool.Activated:Connect(function()
tool:Destroy()
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://18273049494"
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
game.Players.LocalPlayer:SetAttribute("NoHit", true)
game.Players.LocalPlayer:SetAttribute("CanUsingTools", false)
 task.wait(0.5)
 back.Transparency = 1
 sword.Transparency = 0
 local args = {
    [1] = "ChangeTojiWapeon",
    [2] = "MeguminSword"
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
   task.wait(1)
 tools()
   task.wait(2)
 game.Players.LocalPlayer:SetAttribute("NoHit", false)
 game.Players.LocalPlayer:SetAttribute("CanUsingTools", true)
end)

tool.Parent = game.Players.LocalPlayer.Backpack

spawn(function()
repeat task.wait(0.5)
 if sword.Transparency == 0 then
local args = {
    [1] = "ChangeAtribbe",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name),
    [3] = "SpeedChanger",
    [4] = 34
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
  else
local args = {
    [1] = "ChangeAtribbe",
    [2] = workspace:WaitForChild(game.Players.LocalPlayer.Name),
    [3] = "SpeedChanger",
    [4] = 24
}

game:GetService("ReplicatedStorage"):WaitForChild("AllRemoteEvents"):WaitForChild("CLientToServerR"):FireServer(unpack(args))
 end
until game.Players.LocalPlayer.Character.Humanoid.Health <= 0
end)
